---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - entrypoints/popup/App.tsx
autonomous: false
requirements:
  - STOR-01
  - STOR-02

must_haves:
  truths:
    - "Clicking Test Download in the popup triggers a real file save to Downloads/travel-photos/"
    - "Two clicks with the same test filename produce two separate files (no overwrite)"
    - "Extension loads in Chrome developer mode with no manifest errors or console warnings"
    - "Service worker remains functional after 30+ seconds of idle"
  artifacts:
    - path: "entrypoints/popup/App.tsx"
      provides: "Popup UI with test download button wired to background"
      contains: "DOWNLOAD_FILE"
  key_links:
    - from: "entrypoints/popup/App.tsx"
      to: "entrypoints/background.ts"
      via: "chrome.runtime.sendMessage({ type: 'DOWNLOAD_FILE' })"
      pattern: "chrome\\.runtime\\.sendMessage"
    - from: "entrypoints/background.ts"
      to: "lib/download.ts"
      via: "triggerDownload() -> chrome.downloads.download()"
      pattern: "triggerDownload"
---

<objective>
Load the built extension in Chrome and verify the full download pipeline works end-to-end: popup button -> background service worker -> Downloads/travel-photos/ folder.

Purpose: Prove the foundation actually works in a real browser, not just in unit tests. This is the phase gate -- if a file lands in the right folder with collision-safe naming, Phase 1 is done.

Output: Verified working extension ready for Phase 2 to build on.
</objective>

<execution_context>
@/Users/lawrencehuang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lawrencehuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/download.ts:
```typescript
export async function buildSafeFilename(basename: string, ext: string, subfolder: string): Promise<string>;
export async function triggerDownload(url: string, basename: string, ext: string): Promise<number>;
```

From lib/keepalive.ts:
```typescript
export async function keepAlive(): Promise<void>;
```

Message protocol (background.ts listener):
```typescript
// Send from popup:
chrome.runtime.sendMessage({
  type: 'DOWNLOAD_FILE',
  payload: { url: string, basename: string, ext: string }
}, (response) => {
  // response: { ok: boolean, downloadId?: number, error?: string }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extension for dev mode and run all tests</name>
  <files>entrypoints/popup/App.tsx</files>
  <read_first>
    entrypoints/popup/App.tsx
    entrypoints/background.ts
    lib/download.ts
  </read_first>
  <action>
    1. Run `npx vitest run` to confirm all unit tests still pass
    2. Run `npx wxt build` to produce the loadable extension in `.output/chrome-mv3/`
    3. Read the generated `.output/chrome-mv3/manifest.json` and verify it contains:
       - `"manifest_version": 3`
       - `"permissions"` includes `"downloads"` and `"storage"`
       - `"background"` with `"service_worker"` field
       - `"content_scripts"` with `"matches": ["<all_urls>"]`
       - `"action"` with `"default_popup"` pointing to a popup HTML file

    If the popup App.tsx does not already show clear status feedback for success and failure, update it to show:
    - "Downloading..." while the message is in flight
    - "Saved! (ID: {downloadId})" on success
    - "Failed: {error message}" on failure
    - A second click label or counter so the user can see multiple downloads triggered

    This is the last automated step before handing off to the user for manual Chrome verification.
  </action>
  <verify>
    <automated>cd /Users/lawrencehuang/Claude/Projects/photo-extractor && npx vitest run --reporter=verbose 2>&1 | tail -20 && echo "---BUILD---" && npx wxt build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - `npx wxt build` exits with code 0
    - .output/chrome-mv3/manifest.json contains "manifest_version": 3
    - entrypoints/popup/App.tsx contains status feedback text like "Saved" and "Failed"
  </acceptance_criteria>
  <done>
    All tests pass, build succeeds, extension is ready to load in Chrome developer mode.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify extension works end-to-end in Chrome</name>
  <files>entrypoints/popup/App.tsx</files>
  <action>
    User loads the built extension in Chrome developer mode and manually verifies the full download pipeline:
    1. Extension loads with no errors
    2. Test Download button saves file to Downloads/travel-photos/
    3. Second download creates collision-safe filename with _01 suffix
    4. Service worker survives 30+ seconds idle
    5. No console errors or warnings
  </action>
  <verify>User confirms all checks pass</verify>
  <done>Extension verified working in Chrome. Phase 1 complete.</done>
  <what-built>
    Complete Chrome extension with:
    - MV3-compliant manifest with downloads + storage permissions
    - React popup with a "Test Download" button
    - Background service worker that receives download messages and saves files
    - Collision-safe naming (appends _01, _02 on duplicates)
    - Service worker keepalive via chrome.storage.session
    - Content script stub (empty, ready for Phase 2)
  </what-built>
  <how-to-verify>
    1. Open Chrome and go to `chrome://extensions/`
    2. Enable "Developer mode" (toggle in top right)
    3. Click "Load unpacked" and select the folder: `{project_root}/.output/chrome-mv3/`
    4. Verify: Extension appears with name "Photo Extractor" and no error badges

    5. Click the Photo Extractor extension icon in the toolbar to open the popup
    6. Verify: You see a popup with a "Test Download" button (styled with blue background)

    7. Click "Test Download"
    8. Verify: Status shows "Saved!" (or similar success message)
    9. Open Finder and navigate to Downloads/travel-photos/
    10. Verify: A file named `test_photo.jpg` (or similar) exists in that folder

    11. Click "Test Download" again (same button, second time)
    12. Check Downloads/travel-photos/ again
    13. Verify: A second file exists with `_01` suffix (e.g., `test_photo_01.jpg`) -- the first file was NOT overwritten

    14. Wait 35 seconds without interacting with the extension
    15. Click "Test Download" one more time
    16. Verify: Download still works (service worker survived idle period)

    17. Right-click the extension icon -> "Inspect popup" to open DevTools
    18. Check the Console tab for any errors or warnings
    19. Go to `chrome://extensions/` and check for any error badges on Photo Extractor
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Phase 01, Plan 02 verification:
1. Extension loads in Chrome with no manifest errors
2. Test Download button saves a file to Downloads/travel-photos/
3. Second download produces a collision-safe filename (_01 suffix)
4. Service worker survives 30+ seconds of idle
5. No console errors or warnings
</verification>

<success_criteria>
- Extension is loadable in Chrome developer mode
- Downloads land in Downloads/travel-photos/ with correct naming
- Collision safety works (no silent overwrites)
- Service worker stays alive through idle periods
- Phase 1 ROADMAP success criteria 1-4 all verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
