---
phase: 999.3
slug: library-view-sort-saved-photos-by-meta-tags-backlog
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-21
---

# Phase 999.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npx vitest run --reporter=verbose` |
| **Full suite command** | `npx vitest run` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run --reporter=verbose`
- **After every plan wave:** Run `npx vitest run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 999.3-01-T1 | 01 | 1 | LIB-01, LIB-02 (storage data layer) | unit | `npx vitest run tests/unit/library.test.ts --reporter=verbose` | Task self-creates | ⬜ pending |
| 999.3-01-T2 | 01 | 1 | LIB-01 (wire into downloads) | regression | `npx vitest run --reporter=verbose` | Existing suite | ⬜ pending |
| 999.3-02-T1 | 02 | 2 | LIB-03, LIB-04, LIB-05, LIB-06, LIB-07 (UI + sort) | unit + regression | `npx vitest run --reporter=verbose` | tests/unit/library-sort.test.ts (task creates) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No separate Wave 0 plan needed. Plan 01, Task 1 (999.3-01-T1) is a TDD task that self-bootstraps `tests/unit/library.test.ts` as its RED phase before writing production code. Plan 02, Task 1 creates `tests/unit/library-sort.test.ts` for sort logic coverage.

Existing infrastructure covers all phase requirements (vitest + jsdom already installed).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Library icon visible in popup header | LIB-03 | Requires browser extension load | Load extension, open popup, verify library icon renders |
| Clicking library icon navigates to library view | LIB-03 | Requires browser extension load | Load extension, click library icon, verify panel shows |
| Saved photos appear in library after download | LIB-06 | Requires file download + storage | Download a photo via extension, open library, confirm record appears |
| Sort by destination/vendor/category/year works | LIB-04, LIB-05 | Requires visual confirmation of order | Add test records, change sort, verify order updates |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved
