---
phase: 999.3-library-view-sort-saved-photos-by-meta-tags-backlog
plan: 1
subsystem: database
tags: [chrome-storage, typescript, vitest, tdd, lru]

# Dependency graph
requires:
  - phase: 999.2-settings-view-image-quality-control-minimum-resolution-filtering
    provides: AppSettings type, lib/settings.ts pattern for chrome.storage helpers
  - phase: 03-popup-and-naming
    provides: runDownloads in App.tsx, download pipeline structure
provides:
  - SavedPhotoRecord type (lib/library.ts)
  - loadLibrary function (reads chrome.storage.local 'savedPhotos')
  - appendToLibrary function (writes records with 500-record LRU cap)
  - Library persistence wired into App.tsx runDownloads — every successful download logged
affects:
  - 999.3-02 — library UI reads from lib/library.ts using loadLibrary

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Library storage mirrors lib/settings.ts pattern (get/set with typed key, exported constants)
    - TDD used for data layer — test file committed RED before implementation GREEN

key-files:
  created:
    - lib/library.ts
    - tests/unit/library.test.ts
  modified:
    - entrypoints/popup/App.tsx

key-decisions:
  - "appendToLibrary uses array slice from tail (updated.slice(updated.length - MAX_RECORDS)) for LRU — newest entries survive, oldest dropped"
  - "STORAGE_KEY and MAX_RECORDS exported as named constants so tests verify exact values without hardcoding strings"
  - "appendToLibrary call placed after dispatch DOWNLOAD_PROGRESS so progress counter updates before library write — library failure cannot block progress reporting"

patterns-established:
  - "Library data layer pattern: typed interface + STORAGE_KEY constant + loadX/appendX helpers — mirrors lib/settings.ts shape"

requirements-completed: [LIB-01, LIB-02]

# Metrics
duration: 10min
completed: 2026-03-22
---

# Phase 999.3 Plan 01: Library Data Layer Summary

**SavedPhotoRecord type + chrome.storage.local persistence with 500-record LRU cap, wired into runDownloads so every successful download is logged**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-22T08:42:00Z
- **Completed:** 2026-03-22T08:52:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Created `lib/library.ts` with SavedPhotoRecord interface (9 fields), loadLibrary, appendToLibrary, STORAGE_KEY, MAX_RECORDS using TDD
- 10 unit tests covering empty storage, stored records, append behavior, and LRU cap eviction at exactly 500 records
- Wired appendToLibrary into App.tsx runDownloads — fires after each successful download using the original URL and pre-numbered basename

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: library test file** - `0eb59ea` (test)
2. **Task 1 GREEN: library.ts implementation** - `e8abb59` (feat)
3. **Task 2: wire appendToLibrary into runDownloads** - `892a5d8` (feat)

_Note: TDD task split into RED (test) and GREEN (implementation) commits._

## Files Created/Modified
- `lib/library.ts` - SavedPhotoRecord type, loadLibrary, appendToLibrary with LRU cap at MAX_RECORDS (500)
- `tests/unit/library.test.ts` - 10 unit tests covering all behaviors
- `entrypoints/popup/App.tsx` - Added appendToLibrary import and call inside runDownloads

## Decisions Made
- LRU eviction via `slice(updated.length - MAX_RECORDS)` — simple and correct; newest records at tail are preserved
- `STORAGE_KEY` and `MAX_RECORDS` exported so tests verify exact values without duplicating magic strings
- appendToLibrary call placed after `dispatch({ type: 'DOWNLOAD_PROGRESS' })` so the progress counter updates even if library write is slow

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Library data layer complete and tested — Plan 02 can call loadLibrary() to read all saved records for the Library UI
- SavedPhotoRecord fields (destination, vendor, category, year) are available for filtering and sorting in the Library view

---
*Phase: 999.3-library-view-sort-saved-photos-by-meta-tags-backlog*
*Completed: 2026-03-22*
