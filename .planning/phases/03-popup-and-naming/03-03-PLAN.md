---
phase: 03-popup-and-naming
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, NAME-05]

must_haves:
  truths:
    - "Extension builds without errors via npm run build"
    - "Extension loads in Chrome developer mode with no manifest errors"
    - "Opening popup on a travel site and clicking Scan shows extracted images in thumbnail grid"
    - "Clicking thumbnails toggles selection with visible blue ring"
    - "Filling in naming fields and clicking Download saves files with correct filename format"
    - "Downloaded files appear in Downloads/travel-photos/ with destination_vendor_category_index.ext naming"
  artifacts: []
  key_links: []
---

<objective>
Build the extension and verify end-to-end functionality in Chrome: popup UI, image scanning, selection, naming, and download pipeline all working together on a real travel site.

Purpose: Unit tests verify logic in isolation. This plan confirms the full extension works as an integrated product in Chrome. It catches wiring issues between popup, content script, and background service worker that unit tests cannot.
Output: Verified working extension, no new files created
</objective>

<execution_context>
@/Users/lawrencehuang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lawrencehuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-popup-and-naming/03-CONTEXT.md
@.planning/phases/03-popup-and-naming/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extension and fix any build errors</name>
  <files></files>
  <read_first>
    - entrypoints/popup/App.tsx (verify imports resolve correctly)
    - lib/naming.ts (verify exports match what App.tsx imports)
    - package.json (verify build script exists)
  </read_first>
  <action>
    Run `npm run build` to build the extension.

    If build errors occur:
    - TypeScript errors: fix type mismatches in the flagged files
    - Import resolution errors: fix import paths (use `@/lib/naming` pattern consistent with existing code)
    - Tailwind class issues: verify tailwind.config.js content paths include `entrypoints/**/*.tsx`

    After a clean build, verify the output directory exists (`.output/chrome-mv3/` or similar WXT output path).

    Also run `npm test` one final time to confirm no test regressions from any build-related fixes.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no errors
    - Build output directory exists (`.output/` contains chrome-mv3 build)
    - `npm test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Extension builds cleanly. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification in Chrome</name>
  <files></files>
  <action>
    Human verifies the built extension works end-to-end in Chrome. The extension was built in Task 1. Jennifer (or the user) loads it in Chrome developer mode and tests the full workflow: scan, select, name, download.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User confirms popup UI, scanning, selection, naming, and download all work correctly in Chrome.</done>
  <what-built>
    Complete Phase 3 popup UI: thumbnail grid with selection, naming form with datalist category, download button with progress feedback, scan port wiring, and last-used field pre-fill. The extension should be fully functional end-to-end.
  </what-built>
  <how-to-verify>
    1. Load the extension in Chrome:
       - Open `chrome://extensions`
       - Enable "Developer mode" (top right toggle)
       - Click "Load unpacked"
       - Select the `.output/chrome-mv3/` directory

    2. Navigate to a travel site (e.g., booking.com, viator.com, or any image-heavy page like unsplash.com)

    3. Click the Photo Extractor extension icon in the toolbar

    4. Verify idle state:
       - Popup should show "Photo Extractor" header with a "Scan Page" button
       - No images should be loading yet (manual scan trigger)

    5. Click "Scan Page":
       - Should briefly show skeleton loading (gray pulsing tiles)
       - Then thumbnails should appear in a 3-column grid
       - Image count badge should show in header
       - All images should be auto-selected (blue rings + checkmarks)

    6. Test selection:
       - Click a thumbnail to deselect it (ring disappears)
       - Click it again to reselect (ring reappears)
       - Click "Clear All" -- all rings should disappear
       - Click "Select All" -- all rings should reappear

    7. Test naming form:
       - Type "bali" in Destination
       - Type "four-seasons" in Vendor
       - Click into Category -- you should see datalist suggestions (room, pool, lobby, etc.)
       - Type "pool" in Category
       - Optionally type something in Notes
       - The Download button should now show "Download N Photos" and be clickable (blue, not greyed out)

    8. Click Download:
       - Button should show "Downloading..."
       - Status should show "Saving X of N..."
       - On completion: "Saved N photos to Downloads/travel-photos/"

    9. Check Downloads/travel-photos/ folder:
       - Files should be named like `bali_four-seasons_pool_01.jpg`, `bali_four-seasons_pool_02.jpg`, etc.
       - If notes was filled, it should appear in the filename

    10. Close and reopen the popup:
        - Destination, Vendor, and Category should be pre-filled with the last values you typed
        - Notes should be empty (not persisted)
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
- Extension builds without errors
- Popup opens and shows idle state with Scan Page button
- Scanning extracts images into thumbnail grid
- Selection toggles work visually
- Download saves files with correct naming pattern
- Pre-fill works across popup sessions
</verification>

<success_criteria>
- Jennifer can open popup, scan a page, see images, select/deselect them, fill in naming fields, download, and find correctly named files in Downloads/travel-photos/
- Pre-fill persists destination/vendor/category across popup sessions
- No console errors in extension background or popup
</success_criteria>

<output>
After completion, create `.planning/phases/03-popup-and-naming/03-03-SUMMARY.md`
</output>
