---
phase: 999.1-ui-modernization-year-field
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/naming.ts
  - tests/unit/naming.test.ts
autonomous: true
requirements:
  - UIMOD-YEAR

must_haves:
  truths:
    - "buildBasename includes year segment between category and notes in output"
    - "Year field is optional -- omitting year produces the old 3-segment format"
    - "Year value is normalized (only digits survive normalizeField)"
  artifacts:
    - path: "lib/naming.ts"
      provides: "Updated buildBasename with year parameter"
      contains: "year"
    - path: "tests/unit/naming.test.ts"
      provides: "Tests for year parameter in buildBasename"
      contains: "year"
  key_links:
    - from: "lib/naming.ts"
      to: "entrypoints/popup/App.tsx"
      via: "buildBasename import"
      pattern: "buildBasename\\("
---

<objective>
Add a `year` parameter to `buildBasename` in `lib/naming.ts` so filenames follow the format `destination_vendor_category_year[_notes]_index.ext`.

Purpose: The Year field is a new metadata dimension for organizing photos by travel year. The naming function must support it before the UI can wire it in.
Output: Updated `lib/naming.ts` with year parameter, updated tests passing.
</objective>

<execution_context>
@/Users/lawrencehuang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lawrencehuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-ui-modernization-year-field/999.1-CONTEXT.md

<interfaces>
<!-- Current buildBasename signature that must be updated -->

From lib/naming.ts:
```typescript
export function normalizeField(value: string): string;
export function buildBasename(
  destination: string,
  vendor: string,
  category: string,
  notes?: string,
): string;
export function deriveExt(url: string): string;
```

From entrypoints/popup/App.tsx (caller):
```typescript
// Line 669 -- current call site:
const basename = buildBasename(destination, vendor, category, notes);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update buildBasename to accept year parameter and update tests</name>
  <files>lib/naming.ts, tests/unit/naming.test.ts</files>
  <read_first>
    - lib/naming.ts (current buildBasename signature and implementation)
    - tests/unit/naming.test.ts (existing test cases to preserve)
    - entrypoints/popup/App.tsx (caller at line 669 -- must remain compatible)
  </read_first>
  <behavior>
    - buildBasename("bali", "four-seasons", "pool", "2025") produces "bali_four-seasons_pool_2025"
    - buildBasename("bali", "four-seasons", "pool", "2025", "sunset view") produces "bali_four-seasons_pool_2025_sunset-view"
    - buildBasename("bali", "four-seasons", "pool", "2025", "") produces "bali_four-seasons_pool_2025"
    - buildBasename("bali", "four-seasons", "pool", "") produces "bali_four-seasons_pool" (empty year omitted)
    - buildBasename("bali", "four-seasons", "pool", undefined) produces "bali_four-seasons_pool" (undefined year omitted)
    - buildBasename("bali", "four-seasons", "pool") produces "bali_four-seasons_pool" (backward-compatible 3-arg call)
    - All existing tests still pass unchanged (the old 4-arg call with notes as 4th param is now year -- existing callers must be updated)
  </behavior>
  <action>
**New signature:**
```typescript
export function buildBasename(
  destination: string,
  vendor: string,
  category: string,
  year?: string,
  notes?: string,
): string;
```

**Implementation:** Insert `year` between `category` and `notes` in the parts array. Apply the same `if (year && year.trim())` guard used for notes. normalizeField handles the value (digits survive, letters/symbols stripped).

**Update existing tests:** The old 4th positional argument was `notes`. All existing test calls like `buildBasename("bali", "four-seasons", "pool", "sunset view")` must shift `"sunset view"` to the 5th argument position: `buildBasename("bali", "four-seasons", "pool", undefined, "sunset view")`. Update every existing buildBasename test case to use the new 5-arg signature.

**Add new test cases in a "buildBasename with year" describe block:**
1. `buildBasename("bali", "four-seasons", "pool", "2025")` returns `"bali_four-seasons_pool_2025"`
2. `buildBasename("bali", "four-seasons", "pool", "2025", "sunset view")` returns `"bali_four-seasons_pool_2025_sunset-view"`
3. `buildBasename("bali", "four-seasons", "pool", "2025", "")` returns `"bali_four-seasons_pool_2025"` (empty notes omitted)
4. `buildBasename("bali", "four-seasons", "pool", "", "sunset")` returns `"bali_four-seasons_pool_sunset"` (empty year omitted, notes included)
5. `buildBasename("bali", "four-seasons", "pool", undefined, "sunset")` returns `"bali_four-seasons_pool_sunset"` (undefined year omitted)
6. `buildBasename("Bali", "Four Seasons", "Pool", "2025")` returns `"bali_four-seasons_pool_2025"` (normalization applied)

**Update JSDoc:** Change docstring to show new format: `destination_vendor_category[_year][_notes]`

**Do NOT update App.tsx** -- that happens in Plan 03. The old call `buildBasename(destination, vendor, category, notes)` will temporarily treat `notes` as `year`, but this is acceptable because Plan 03 rewrites that call before any user-facing build.
  </action>
  <verify>
    <automated>cd /Users/lawrencehuang/Claude/Projects/photo-extractor && npx vitest run tests/unit/naming.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/naming.ts contains `year?: string` in buildBasename signature
    - lib/naming.ts buildBasename function body contains `if (year && year.trim())`
    - tests/unit/naming.test.ts contains `buildBasename with year` describe block
    - tests/unit/naming.test.ts contains test case with string `"bali_four-seasons_pool_2025"`
    - tests/unit/naming.test.ts contains test case with string `"bali_four-seasons_pool_2025_sunset-view"`
    - `npx vitest run tests/unit/naming.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>buildBasename accepts 5 args (destination, vendor, category, year?, notes?), all existing tests updated and passing, new year test cases passing</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/naming.test.ts` passes with 0 failures
- `grep "year" lib/naming.ts` shows year parameter in signature and implementation
</verification>

<success_criteria>
- buildBasename produces `destination_vendor_category_year_notes_index.ext` format when year is provided
- buildBasename still produces `destination_vendor_category_notes_index.ext` when year is empty/undefined
- All existing naming tests pass (backward compatibility preserved)
- New year-specific tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-ui-modernization-year-field/999.1-01-SUMMARY.md`
</output>
