---
phase: 999.1-ui-modernization-year-field
verified: 2026-03-21T13:39:00Z
status: human_needed
score: 12/12 automated must-haves verified
human_verification:
  - test: "Load the built extension in Chrome and open the popup on any travel website"
    expected: "Header shows blue camera icon (32px), 'Photo Extractor' in Manrope bold, and 'Scan Page' button with light-blue MD3 styling"
    why_human: "Visual rendering of CSS classes (backdrop-blur, font loading, color tokens) cannot be verified programmatically"
  - test: "Click 'Scan Page' and wait for images to load"
    expected: "Gallery header shows a count badge (e.g. '12'), 'Gallery' heading in Manrope extrabold, and 'Select All' / 'Clear All' links on the right"
    why_human: "Gallery header layout and typography require visual inspection"
  - test: "Click a thumbnail to select it, then inspect its appearance"
    expected: "Selected thumbnail has a blue ring border, a subtle blue tint overlay, and a green circle with white checkmark in the top-right corner"
    why_human: "Selection state styling (ring-2 ring-primary + bg-tertiary) requires visual inspection"
  - test: "Inspect the metadata form area"
    expected: "Form is inside a light-gray rounded card with 'METADATA ASSIGNMENT' label in small uppercase text. Category shows a dropdown (not a text field). Year shows a number input next to Category. Notes shows a multi-line textarea."
    why_human: "Form card surface, select vs input rendering, and textarea rows require visual inspection"
  - test: "Fill in all fields and click the download button"
    expected: "Download button is full-width with blue-tinted background (primary-container), download icon, and Manrope extrabold text. Gradient fades up from the bottom behind the button."
    why_human: "Sticky footer gradient and button shadow require visual inspection"
  - test: "Download a photo, then check the filename in the Downloads/travel-photos/ folder"
    expected: "Filename includes year between category and notes, e.g. bali_four-seasons_landscape_2026_01.jpg"
    why_human: "Filename output requires triggering an actual download"
  - test: "Close the popup and reopen it on the same site"
    expected: "Destination, vendor, category, and year fields restore the values from the previous session"
    why_human: "chrome.storage.local persistence round-trip requires manual testing"
  - test: "Open the popup on a new tab (chrome://newtab) and click Scan Page"
    expected: "Popup shows 'Can't scan this page' message with an explanation, not a blank or frozen UI"
    why_human: "Restricted-page guard behavior requires manual testing in a browser"
  - test: "Confirm no bottom navigation bar appears anywhere in the popup"
    expected: "No Library or Settings tabs are visible at the bottom of the popup"
    why_human: "Absence of deferred UI elements requires visual inspection"
---

# Phase 999.1: UI Modernization + Year Field Verification Report

**Phase Goal:** Modernize the popup UI with Material Design 3 and add Year field to metadata form and filename output.
**Verified:** 2026-03-21T13:39:00Z
**Status:** human_needed — all automated checks passed; visual and behavioral checks require human testing in Chrome
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | buildBasename includes year segment between category and notes | VERIFIED | `lib/naming.ts` line 87-89: `if (year && year.trim()) { parts.push(normalizeField(year)); }` — year inserted after category, before notes |
| 2 | Year field is optional — omitting year produces 3-segment format | VERIFIED | Guard pattern `if (year && year.trim())` skips year when blank; 37 tests pass including cases with undefined/empty year |
| 3 | Year value is normalized (only digits survive normalizeField) | VERIFIED | `normalizeField` strips non-`[a-z0-9-]` characters; a year like "2025" passes through unchanged (digits survive) |
| 4 | Tailwind config has MD3 color tokens usable as classes | VERIFIED | `tailwind.config.js` lines 8-37: full MD3 token set including `primary-container`, `surface-container-low`, `tertiary`, etc. |
| 5 | Manrope and Inter fonts load in extension popup | VERIFIED | `public/fonts/manrope-latin.woff2` (24KB WOFF2) and `public/fonts/inter-latin.woff2` (48KB WOFF2) exist; `style.css` has @font-face declarations pointing to `/fonts/` path; both appear in build output |
| 6 | font-manrope and font-inter utility classes available | VERIFIED | `tailwind.config.js` lines 41-43: fontFamily entries for `manrope` and `inter` |
| 7 | Popup header has camera icon, Manrope bold title, and Scan Page button | VERIFIED | `App.tsx` lines 847-866: `backdrop-blur-md` sticky header, `font-manrope` title, `bg-primary-container` scan button |
| 8 | Gallery has count badge, Select All / Clear All, 3-col grid with MD3 selection | VERIFIED | `App.tsx` lines 560-581: GalleryHeader with `bg-secondary-fixed` badge, Select All / Clear All buttons; ThumbnailCard lines 431-446: `ring-2 ring-primary` + `bg-primary/10` overlay + `bg-tertiary` checkmark |
| 9 | Metadata form is in surface-container-low card with Category as select and Year as number input | VERIFIED | `App.tsx` lines 605-696: card wrapper `bg-surface-container-low p-5 rounded-xl`, `<select>` for category with 4 options, `type="number"` input for year in 2-col grid |
| 10 | Download button is full-width with MD3 primary-container styling and sticky footer gradient | VERIFIED | `App.tsx` lines 761-776: `sticky bottom-0 bg-gradient-to-t from-surface`, button `bg-primary-container text-on-primary-container font-manrope font-extrabold py-4 rounded-xl shadow-lg shadow-primary/20` |
| 11 | Year field value is passed to buildBasename and persisted to chrome.storage.local | VERIFIED | `App.tsx` line 756: `buildBasename(destination, vendor, category, year, notes)` — 5 args; lines 372-378: `chrome.storage.local.set` includes `year: state.year`; line 887: storage read includes `'year'`; PREFILL_LOADED reducer (line 195) restores `year` |
| 12 | No bottom navigation bar is rendered | VERIFIED | Full `App.tsx` reviewed (995 lines); no nav bar, Library tab, or Settings tab markup anywhere |

**Score:** 12/12 truths verified by static analysis and test execution

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/naming.ts` | Updated buildBasename with year parameter | VERIFIED | 5-arg signature `(destination, vendor, category, year?, notes?)` at line 75; `if (year && year.trim())` guard at line 87 |
| `tests/unit/naming.test.ts` | Tests for year parameter in buildBasename | VERIFIED | `buildBasename with year` describe block at line 154; 6 test cases; all 37 tests pass |
| `tailwind.config.js` | MD3 color tokens, font families, border radius overrides | VERIFIED | `primary-container`, `surface-container-low`, `tertiary`, `manrope`, `inter` all present |
| `entrypoints/popup/style.css` | @font-face declarations for Manrope and Inter | VERIFIED | 2 @font-face blocks at lines 5-20; both reference `/fonts/` paths |
| `public/fonts/manrope-latin.woff2` | Manrope font file bundled with extension | VERIFIED | 24,576 bytes; `file` command confirms WOFF2 format |
| `public/fonts/inter-latin.woff2` | Inter font file bundled with extension | VERIFIED | 48,432 bytes; `file` command confirms WOFF2 format |
| `entrypoints/popup/App.tsx` | Complete modernized popup UI | VERIFIED | 995 lines; contains `font-manrope` (lines 568, 854, 767), all MD3 token classes |
| `entrypoints/popup/App.tsx` | Year field in state and form | VERIFIED | `year: string` in PopupState (line 49), initialState (line 84), FIELD_CHANGE union (line 68), PREFILL_LOADED action (line 72), NamingForm props (line 587), DownloadButton props (line 704) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lib/naming.ts` | `entrypoints/popup/App.tsx` | `buildBasename` import with year parameter | VERIFIED | `App.tsx` line 27: `import { buildBasename, deriveExt } from '../../lib/naming'`; line 756: `buildBasename(destination, vendor, category, year, notes)` — year is 4th arg |
| `entrypoints/popup/App.tsx` | `chrome.storage.local` | year field persistence | VERIFIED | Line 372-378: `chrome.storage.local.set({ ..., year: state.year, ... })`; line 887: `chrome.storage.local.get(['destination', 'vendor', 'category', 'year', 'lastHostname'])`; PREFILL_LOADED reducer restores year |
| `tailwind.config.js` | `entrypoints/popup/App.tsx` | MD3 token classes used in TSX | VERIFIED | `font-manrope`, `bg-primary-container`, `bg-surface-container-low`, `bg-tertiary`, `text-on-surface-variant`, `backdrop-blur-md` all present in App.tsx |
| `entrypoints/popup/style.css` | `public/fonts/` | @font-face src url | VERIFIED | `url('/fonts/manrope-latin.woff2')` and `url('/fonts/inter-latin.woff2')` match actual files in `public/fonts/`; both appear in `.output/chrome-mv3/fonts/` after build |

---

### Requirements Coverage

The plans declare UIMOD-* requirement IDs (UIMOD-YEAR, UIMOD-TOKENS, UIMOD-FONTS, UIMOD-HEADER, UIMOD-GALLERY, UIMOD-FORM, UIMOD-FOOTER). These IDs are defined in `ROADMAP.md` line 120 but are **not registered in `REQUIREMENTS.md`**. This is an informational gap — the IDs exist as roadmap-level labels only, not as formal requirement entries with descriptions in the requirements document.

Impact: traceability is intact at the ROADMAP level; the REQUIREMENTS.md traceability table does not need updating for backlog phases (999.x) because those phases extend functionality beyond the v1 requirements baseline.

| Requirement ID | Defined In | Plan | Implementation Evidence | Status |
|---------------|-----------|------|------------------------|--------|
| UIMOD-YEAR | ROADMAP.md | 999.1-01, 999.1-03 | `buildBasename` year param; Year field in App.tsx state, form, buildBasename call, storage | SATISFIED |
| UIMOD-TOKENS | ROADMAP.md | 999.1-02 | Full MD3 token set in `tailwind.config.js` | SATISFIED |
| UIMOD-FONTS | ROADMAP.md | 999.1-02 | WOFF2 files in `public/fonts/`; @font-face in `style.css`; font utility classes in Tailwind config | SATISFIED |
| UIMOD-HEADER | ROADMAP.md | 999.1-03 | Sticky header with `backdrop-blur-md`, camera SVG, `font-manrope` title, `bg-primary-container` scan button | SATISFIED |
| UIMOD-GALLERY | ROADMAP.md | 999.1-03 | GalleryHeader with count badge, Select All / Clear All; ThumbnailCard with `ring-2 ring-primary` + `bg-tertiary` checkmark | SATISFIED |
| UIMOD-FORM | ROADMAP.md | 999.1-03 | Form card `bg-surface-container-low`; `<select>` for Category; `type="number"` for Year; `<textarea rows={2}>` for Notes | SATISFIED |
| UIMOD-FOOTER | ROADMAP.md | 999.1-03 | Sticky `bg-gradient-to-t from-surface` footer; `bg-primary-container font-manrope font-extrabold py-4 rounded-xl shadow-lg` button | SATISFIED |

---

### Anti-Patterns Found

No blocking or warning-level anti-patterns found.

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| `App.tsx` line 797 | `return null` in StatusMessage | Info | Legitimate early return when downloadStatus is 'idle' — not a stub |
| `App.tsx` lines 622, 637, 690 | `placeholder="..."` attributes | Info | HTML input placeholder text — not implementation stubs |

---

### Human Verification Required

All automated checks passed. The following items require loading the built extension in Chrome:

**1. Header visual rendering**

Test: Load the extension popup on any travel website.
Expected: Blue camera icon (32px), "Photo Extractor" in Manrope bold, "Scan Page" button with light-blue `#D3E3FD` background and dark-blue `#062E6F` text.
Why human: Font loading, backdrop-blur, and color token rendering cannot be verified from source alone.

**2. Gallery section layout**

Test: Click "Scan Page" and wait for images.
Expected: Photo count badge (secondary-fixed), "Gallery" in Manrope extrabold, "Select All" / "Clear All" links right-aligned. Gallery is capped at ~210px height with scroll.
Why human: Manrope font render weight and layout at runtime.

**3. Selected thumbnail state**

Test: Click a thumbnail.
Expected: Blue ring border, subtle blue tint overlay, green circle with white checkmark in top-right corner.
Why human: CSS ring, overlay, and icon placement require visual confirmation.

**4. Metadata form appearance**

Test: Inspect the form area after scan.
Expected: Light-gray rounded card, "METADATA ASSIGNMENT" in small uppercase tracking, Category as a `<select>` dropdown, Year as a number spinner, Notes as a 2-row textarea.
Why human: Visual distinction between input types and card styling.

**5. Download button and gradient footer**

Test: Fill in all required fields, check the button state.
Expected: Full-width button with primary-container blue tint, Manrope extrabold text, colored shadow, and a gradient fade-in from the bottom.
Why human: CSS gradient, shadow, and button styling.

**6. Filename includes year**

Test: Download a photo. Check the filename in Downloads/travel-photos/.
Expected: Year appears between category and notes, e.g. `bali_four-seasons_landscape_2026_01.jpg`.
Why human: Actual download must be triggered to confirm filename output.

**7. Year persists across popup sessions**

Test: Fill in Year (e.g. 2024), download a photo, close and reopen the popup on the same site.
Expected: Year field restores to 2024.
Why human: chrome.storage.local round-trip requires a live browser session.

**8. Restricted page guard**

Test: Open popup on chrome://newtab, click "Scan Page".
Expected: "Can't scan this page" message with explanation text. No crash or silent freeze.
Why human: Requires a live browser with the extension loaded.

**9. No bottom navigation bar**

Test: Inspect the full popup height in all states (idle, scanning, done with results).
Expected: No Library or Settings tab bar anywhere.
Why human: Negative visual confirmation.

---

### Notes

- UIMOD-* requirement IDs are defined in ROADMAP.md but not registered as formal entries in REQUIREMENTS.md. This is acceptable for backlog phases extending beyond the v1 baseline. No action required.
- Plan 03 added a `'restricted'` ScanStatus and SCAN_RESTRICTED action not specified in the original plan, as a fix caught during the required human verification gate. This is a beneficial addition, not scope creep.
- NamingForm and DownloadButton are only rendered when `showGallery === true` (scan done + images exist). The Year field and download wiring are therefore also only active in that state — this matches the intended UX.

---

_Verified: 2026-03-21T13:39:00Z_
_Verifier: Claude (gsd-verifier)_
