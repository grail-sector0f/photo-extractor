---
phase: 999.1-ui-modernization-year-field
plan: 01
subsystem: ui
tags: [typescript, naming, tdd, vitest]

# Dependency graph
requires: []
provides:
  - buildBasename updated to accept year as optional 4th parameter between category and notes
  - Format: destination_vendor_category[_year][_notes]
affects:
  - 999.1-02 (UI wiring — will read the new signature)
  - 999.1-03 (App.tsx call site update — must shift notes to 5th arg)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional year field: if (year && year.trim()) guard before normalizeField — same pattern as existing notes"

key-files:
  created: []
  modified:
    - lib/naming.ts
    - tests/unit/naming.test.ts

key-decisions:
  - "year inserted between category and notes in the parts array — matches target filename format destination_vendor_category_year_notes"
  - "App.tsx call site not updated in this plan — notes temporarily treated as year until Plan 03 rewires the call"

patterns-established:
  - "TDD pattern: RED commit (failing tests) then GREEN commit (implementation) per task"

requirements-completed:
  - UIMOD-YEAR

# Metrics
duration: 5min
completed: 2026-03-21
---

# Phase 999.1 Plan 01: Year Parameter in buildBasename Summary

**Added `year` as optional 4th parameter to `buildBasename`, inserting it between category and notes so filenames follow `destination_vendor_category_year[_notes]` format.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-21T09:43:00Z
- **Completed:** 2026-03-21T09:44:30Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Updated `buildBasename` signature from 4-arg to 5-arg form (`year?` inserted as 4th, `notes?` shifted to 5th)
- Implemented year segment with same guard pattern as notes: `if (year && year.trim())`
- Updated JSDoc and file-level docstring to document the new `destination_vendor_category[_year][_notes]` format
- Updated all existing buildBasename tests to use the 5-arg form (notes shifted to 5th position)
- Added new `buildBasename with year` describe block with 6 test cases covering year-only, year+notes, empty year, empty notes, and undefined year scenarios
- All 37 tests pass

## Task Commits

TDD task with two commits:

1. **RED — Failing tests:** `fa36695` (test)
2. **GREEN — Implementation:** `fccdc1a` (feat)

## Files Created/Modified

- `/Users/lawrencehuang/Claude/Projects/photo-extractor/lib/naming.ts` — Added `year?: string` parameter, `if (year && year.trim())` guard, updated docs
- `/Users/lawrencehuang/Claude/Projects/photo-extractor/tests/unit/naming.test.ts` — Updated existing buildBasename tests to 5-arg form; added `buildBasename with year` describe block

## Decisions Made

- year parameter inserted before notes (4th position) to match the target filename format `destination_vendor_category_year_notes`
- App.tsx call site deliberately left unchanged — it currently passes `notes` as the 4th arg which will be interpreted as `year` until Plan 03 rewires the call; this is acceptable because no user-facing build runs between plans

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `buildBasename` accepts 5 args and all tests pass — naming layer is ready for Plan 02 (Year field in UI) and Plan 03 (App.tsx call site update)
- App.tsx at line 669 still uses the old 4-arg call — Plan 03 must shift `notes` to 5th arg and add `year` as 4th

---
*Phase: 999.1-ui-modernization-year-field*
*Completed: 2026-03-21*
