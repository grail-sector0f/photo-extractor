# Phase 999.1: UI Modernization + Year Field - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-21
**Phase:** 999.1 — library-view-sort-saved-photos-by-meta-tags-destination-vendor-category-year
**Areas discussed:** Category field, Year default, Bottom nav, Font loading, Year in filename, Year persistence

---

## Scope clarification

The user clarified that Phase 999.1 is NOT building a library/gallery/settings screen.
The phase is: **UI modernization based on the high-fidelity mockup + adding a Year field**.
Library, gallery browse, and settings are explicitly deferred.

The mockup HTML was provided inline during discussion and serves as the design source of truth.

---

## Category field

| Option | Description | Selected |
|--------|-------------|----------|
| Locked select (mockup) | Dropdown with fixed options: Landscape, Accommodation, Dining, Activities | ✓ |
| Keep free-text with datalist | Jennifer types anything, datalist suggestions appear | |

**User's choice:** Locked select (mockup)
**Notes:** Replaces the previous free-text input + datalist approach from Phase 3.

---

## Year default

| Option | Description | Selected |
|--------|-------------|----------|
| Current year (auto-detected) | Defaults to actual current year via `new Date().getFullYear()` | ✓ |
| Fixed default | Always opens with same hardcoded year | |

**User's choice:** Current year (auto-detected)

---

## Bottom navigation bar

| Option | Description | Selected |
|--------|-------------|----------|
| Include nav, disable Library + Settings | Nav bar renders, non-Gallery tabs grayed out and non-functional | |
| Skip nav bar entirely | No nav bar in this phase | ✓ |

**User's choice:** Skip nav bar entirely

---

## Font loading

| Option | Description | Selected |
|--------|-------------|----------|
| Bundle font files in the extension | Download Manrope + Inter WOFF2, include in /public | ✓ |
| Use system font stack as fallback | Skip custom fonts, use system fonts | |

**User's choice:** Bundle font files in extension

---

## Year in filename

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — include year in filename | destination_vendor_category_year_notes_index.ext | ✓ |
| No — metadata only | Year stored but not in filename | |

**User's choice:** Include year in filename

---

## Year persistence

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — persist year | Restored on popup open like other fields | ✓ |
| No — always default to current year | Resets to current year every open | |

**User's choice:** Persist year

---

## Claude's Discretion

- Exact WOFF2 subsets to bundle
- Font loading mechanism (@font-face CSS vs inline)
- Material Symbols icon strategy (CDN unavailable in extension)
- Select dropdown custom styling approach
- Transition/animation implementation details

## Deferred Ideas

- Library screen — future phase
- Settings screen — future phase
- Bottom navigation bar — deferred until Library/Settings phases exist
