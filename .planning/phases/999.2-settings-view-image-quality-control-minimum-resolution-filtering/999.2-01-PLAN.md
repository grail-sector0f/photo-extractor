---
phase: 999.2-settings-view-image-quality-control-minimum-resolution-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/settings.ts
  - lib/extract/imgTags.ts
  - entrypoints/content.ts
  - entrypoints/popup/App.tsx
  - tests/unit/settings.test.ts
  - tests/unit/contentScript.test.ts
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04
  - SETT-05

must_haves:
  truths:
    - "Settings module exports AppSettings type, DEFAULT_SETTINGS, loadSettings, and saveSettings"
    - "loadSettings returns defaults when chrome.storage.local has no settings key"
    - "saveSettings persists settings object under the 'settings' key in chrome.storage.local"
    - "Content script uses minDimension from SCAN_PAGE message instead of hardcoded 100"
    - "GIF images are excluded from scan results when skipGifs is true"
    - "CDN rewrite is skipped when cdnUpscalingEnabled is false"
  artifacts:
    - path: "lib/settings.ts"
      provides: "Settings types, defaults, load/save helpers"
      exports: ["AppSettings", "DEFAULT_SETTINGS", "loadSettings", "saveSettings"]
    - path: "tests/unit/settings.test.ts"
      provides: "Unit tests for settings module"
      min_lines: 30
  key_links:
    - from: "entrypoints/popup/App.tsx"
      to: "lib/settings.ts"
      via: "import DEFAULT_SETTINGS, loadSettings, saveSettings"
      pattern: "import.*from.*lib/settings"
    - from: "entrypoints/content.ts"
      to: "SCAN_PAGE message payload"
      via: "msg.minDimension ?? DEFAULT_SETTINGS.minDimension"
      pattern: "minDimension"
    - from: "entrypoints/popup/App.tsx"
      to: "lib/cdnRewrite.ts"
      via: "conditional rewrite based on cdnUpscalingEnabled"
      pattern: "cdnUpscalingEnabled"
---

<objective>
Create the settings data layer: types, defaults, chrome.storage persistence helpers, and wire settings into the content script scan pipeline (minDimension), popup GIF filtering, and CDN toggle at download time.

Purpose: Establish the settings contract and plumbing so that Plan 02 can build a UI that reads/writes these settings without touching extraction or download logic.
Output: `lib/settings.ts` with full test coverage, content script accepting configurable minDimension, popup applying GIF filter and CDN toggle.
</objective>

<execution_context>
@/Users/lawrencehuang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lawrencehuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-settings-view-image-quality-control-minimum-resolution-filtering/999.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/extract/types.ts:
```typescript
export interface ImageResult {
  url: string;
  sourceType: 'img' | 'css-bg';
  naturalWidth: number;
  naturalHeight: number;
}
```

From lib/naming.ts:
```typescript
export function deriveExt(url: string): string;
// KNOWN_EXTENSIONS = ['jpg', 'jpeg', 'png', 'webp', 'gif', 'avif']
```

From lib/cdnRewrite.ts:
```typescript
export function rewriteUrlForMaxResolution(url: string): string;
```

From entrypoints/popup/App.tsx (current state shape):
```typescript
interface PopupState {
  scanStatus: ScanStatus;
  images: ImageResult[];
  blobCount: number;
  selected: Set<string>;
  destination: string;
  vendor: string;
  category: string;
  year: string;
  notes: string;
  downloadStatus: DownloadStatus;
  downloadProgress: { done: number; total: number };
  downloadSaved: number;
  downloadFailed: number;
}
```

From entrypoints/content.ts (current scan message handling):
```typescript
// In handleScanSession, port.onMessage listener:
if (message.type !== 'SCAN_PAGE') return;
const imgResults = extractImgTags();
// MIN_DIMENSION = 100 is hardcoded at line 41
```

From lib/extract/imgTags.ts:
```typescript
const MIN_DIMENSION = 100; // line 28 -- hardcoded
export function extractImgTags(): ImageResult[];
```

From tests/setup.ts:
```typescript
// chrome.storage.local mock already exists with get/set
chromeMock.storage.local.get.mockResolvedValue({});
chromeMock.storage.local.set.mockResolvedValue(undefined);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create settings module with types, defaults, and storage helpers</name>
  <files>lib/settings.ts, tests/unit/settings.test.ts</files>
  <read_first>
    - tests/setup.ts (chrome.storage.local mock pattern)
    - lib/naming.ts (code style reference for pure module)
    - tests/unit/naming.test.ts (test style reference)
  </read_first>
  <behavior>
    - loadSettings() returns DEFAULT_SETTINGS when chrome.storage.local.get('settings') returns {}
    - loadSettings() merges stored partial settings over defaults (e.g., stored {minDimension: 300} returns full object with minDimension=300 and other fields at defaults)
    - saveSettings({...}) calls chrome.storage.local.set with { settings: {...} }
    - DEFAULT_SETTINGS.minDimension is 150 (user decision: Medium preset)
    - DEFAULT_SETTINGS.skipGifs is false (user decision: off by default)
    - DEFAULT_SETTINGS.cdnUpscalingEnabled is true (user decision: on by default)
    - AppSettings interface has exactly three fields: minDimension (number), skipGifs (boolean), cdnUpscalingEnabled (boolean)
    - MIN_DIMENSION_PRESETS exports three presets: { label: 'Small', value: 50 }, { label: 'Medium', value: 150 }, { label: 'Large', value: 300 }
  </behavior>
  <action>
    Create lib/settings.ts with:

    1. `AppSettings` interface with three fields:
       - `minDimension: number` (minimum pixel dimension for scan filtering)
       - `skipGifs: boolean` (exclude .gif URLs from scan results)
       - `cdnUpscalingEnabled: boolean` (apply CDN URL rewrites at download)

    2. `MIN_DIMENSION_PRESETS` constant array (per user decision -- preset buttons, not a slider):
       ```typescript
       export const MIN_DIMENSION_PRESETS = [
         { label: 'Small', value: 50 },
         { label: 'Medium', value: 150 },
         { label: 'Large', value: 300 },
       ] as const;
       ```

    3. `DEFAULT_SETTINGS` constant:
       - minDimension: 150 (user decision: default Medium)
       - skipGifs: false (user decision: off by default)
       - cdnUpscalingEnabled: true (user decision: on by default)

    4. `loadSettings()`: async function that reads chrome.storage.local.get('settings') and merges with DEFAULT_SETTINGS using `{ ...DEFAULT_SETTINGS, ...(stored.settings ?? {}) }` pattern. Returns Promise<AppSettings>.

    5. `saveSettings(settings: AppSettings)`: async function that writes to chrome.storage.local.set({ settings }).

    Create tests/unit/settings.test.ts with tests for all behaviors listed above. The chrome.storage.local mock from tests/setup.ts is already available globally.
  </action>
  <verify>
    <automated>npm test -- settings</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export interface AppSettings" lib/settings.ts
    - grep -q "minDimension: number" lib/settings.ts
    - grep -q "skipGifs: boolean" lib/settings.ts
    - grep -q "cdnUpscalingEnabled: boolean" lib/settings.ts
    - grep -q "minDimension: 150" lib/settings.ts
    - grep -q "skipGifs: false" lib/settings.ts
    - grep -q "cdnUpscalingEnabled: true" lib/settings.ts
    - grep -q "MIN_DIMENSION_PRESETS" lib/settings.ts
    - grep -q "export async function loadSettings" lib/settings.ts
    - grep -q "export async function saveSettings" lib/settings.ts
    - grep -q "DEFAULT_SETTINGS" lib/settings.ts
    - npm test -- settings passes with 0 failures
  </acceptance_criteria>
  <done>lib/settings.ts exports AppSettings, DEFAULT_SETTINGS (minDimension=150, skipGifs=false, cdnUpscalingEnabled=true), MIN_DIMENSION_PRESETS, loadSettings, saveSettings. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings into content script, popup GIF filter, and CDN toggle</name>
  <files>lib/extract/imgTags.ts, entrypoints/content.ts, entrypoints/popup/App.tsx, tests/unit/contentScript.test.ts</files>
  <read_first>
    - lib/settings.ts (just created in Task 1 -- the settings contract)
    - lib/extract/imgTags.ts (current MIN_DIMENSION hardcode at line 28)
    - entrypoints/content.ts (current MIN_DIMENSION hardcode at line 41, handleScanSession, processImg)
    - entrypoints/popup/App.tsx (current startScan at ~line 242, runDownloads at ~line 327, useEffect prefill at ~line 922, PopupState/Action types, popupReducer)
    - tests/unit/contentScript.test.ts (existing test patterns for content script)
    - tests/setup.ts (mock port factory)
  </read_first>
  <action>
    **A. Remove hardcoded MIN_DIMENSION from imgTags.ts (line 28):**
    - Change `extractImgTags()` signature to `extractImgTags(minDimension: number = 150)`.
    - Replace the `const MIN_DIMENSION = 100` with the parameter. The default 150 matches DEFAULT_SETTINGS.minDimension.

    **B. Update content.ts processImg and handleScanSession:**
    - Import `DEFAULT_SETTINGS` from `@/lib/settings`.
    - In `processImg`: change signature to accept `minDimension` parameter: `processImg(img, seenUrls, minDimension = DEFAULT_SETTINGS.minDimension)`. Remove the local `const MIN_DIMENSION = 100` at line 41. Use the parameter in the dimension check.
    - In `handleScanSession`, in the SCAN_PAGE message handler:
      - Extract minDimension from message: `const minDim = (msg as { minDimension?: number }).minDimension ?? DEFAULT_SETTINGS.minDimension;`
      - Pass it to `extractImgTags(minDim)` and `processImg(img, seenUrls, minDim)` (in the MutationObserver callback, capture `minDim` in closure).

    **C. Update App.tsx -- add settings to state and reducer:**
    - Import `AppSettings`, `DEFAULT_SETTINGS`, `loadSettings` from `../../lib/settings`.
    - Import `deriveExt` (already imported) -- will be used for GIF filtering.
    - Add `settings: AppSettings` field to `PopupState` interface, defaulting to `DEFAULT_SETTINGS` in `initialState`.
    - Add `SETTINGS_LOADED` action: `{ type: 'SETTINGS_LOADED'; settings: AppSettings }`.
    - Handle in reducer: `case 'SETTINGS_LOADED': return { ...state, settings: action.settings };`

    **D. Update App.tsx -- merge settings load into existing prefill useEffect:**
    - In the existing `useEffect` on mount, change the `chrome.storage.local.get` call to also include `'settings'` in the keys array.
    - After the existing prefill logic, merge settings: `const settings = { ...DEFAULT_SETTINGS, ...(data.settings ?? {}) }; dispatch({ type: 'SETTINGS_LOADED', settings });`
    - This replaces the two-call anti-pattern with a single storage read (per RESEARCH.md Pitfall 2).

    **E. Update App.tsx startScan -- pass minDimension in SCAN_PAGE message:**
    - Change `startScan` to accept `settings: AppSettings` as second parameter (after dispatch).
    - At line where `port.postMessage({ type: 'SCAN_PAGE' })` is called, change to: `port.postMessage({ type: 'SCAN_PAGE', minDimension: settings.minDimension })`.
    - Update `handleScan` callback in App component to pass `state.settings`: `startScan(dispatch, state.settings)`.

    **F. Update App.tsx -- apply GIF filter on scan results:**
    - In the `SCAN_RESULT` reducer case, before auto-selecting: if `state.settings.skipGifs` is true, filter out images where `deriveExt(img.url) === 'gif'`.
    - Problem: reducer is pure and doesn't have access to `deriveExt` import context easily from within the switch. Instead, apply the filter OUTSIDE the reducer:
      - In the `port.onMessage.addListener` callback inside `startScan`, after receiving SCAN_RESULT, filter images before dispatching:
        ```typescript
        let filteredImages = images;
        if (settings.skipGifs) {
          filteredImages = images.filter(img => deriveExt(img.url) !== 'gif');
        }
        dispatch({ type: 'SCAN_RESULT', payload: { images: filteredImages, blobCount } });
        ```
      - Similarly for IMAGE_FOUND messages, check before dispatching.

    **G. Update App.tsx runDownloads -- conditional CDN rewrite:**
    - Change the CDN rewrite line from:
      `const upscaledUrl = rewriteUrlForMaxResolution(url);`
      to:
      `const upscaledUrl = state.settings.cdnUpscalingEnabled ? rewriteUrlForMaxResolution(url) : url;`
    - Update the conditional below: since `upscaledUrl === url` is already the fast-path check, this change naturally handles the toggle (when disabled, upscaledUrl always equals url, so the fast path fires).

    **H. Add test cases to contentScript.test.ts:**
    - Test that handleScanSession passes custom minDimension to extractImgTags when SCAN_PAGE includes minDimension.
    - Test that processImg uses provided minDimension parameter instead of hardcoded 100.
    - Test that missing minDimension in SCAN_PAGE falls back to DEFAULT_SETTINGS.minDimension (150).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "minDimension" lib/extract/imgTags.ts (parameter, not hardcoded constant)
    - grep -v "const MIN_DIMENSION" lib/extract/imgTags.ts (hardcoded constant removed)
    - grep -v "const MIN_DIMENSION" entrypoints/content.ts (hardcoded constant removed)
    - grep -q "minDimension" entrypoints/content.ts (uses parameter from message)
    - grep -q "DEFAULT_SETTINGS" entrypoints/content.ts (imports from settings module)
    - grep -q "settings: AppSettings" entrypoints/popup/App.tsx (settings in state)
    - grep -q "SETTINGS_LOADED" entrypoints/popup/App.tsx (new action type)
    - grep -q "minDimension.*settings" entrypoints/popup/App.tsx (passes minDimension in scan message)
    - grep -q "cdnUpscalingEnabled" entrypoints/popup/App.tsx (conditional CDN rewrite)
    - grep -q "skipGifs" entrypoints/popup/App.tsx (GIF filtering logic)
    - npm test passes with 0 failures
  </acceptance_criteria>
  <done>Hardcoded MIN_DIMENSION removed from both imgTags.ts and content.ts. Content script reads minDimension from SCAN_PAGE message. Popup loads settings on mount, passes minDimension to scan, filters GIFs when skipGifs=true, skips CDN rewrite when cdnUpscalingEnabled=false. All existing and new tests pass.</done>
</task>

</tasks>

<verification>
1. `npm test` passes with 0 failures
2. `grep -r "const MIN_DIMENSION" lib/ entrypoints/` returns no results (hardcoded constants removed)
3. `grep -q "loadSettings\|saveSettings\|DEFAULT_SETTINGS\|AppSettings" lib/settings.ts` confirms all exports exist
4. Settings module is a pure dependency with no side effects on import
</verification>

<success_criteria>
- lib/settings.ts exists with AppSettings type, DEFAULT_SETTINGS (minDimension=150, skipGifs=false, cdnUpscalingEnabled=true), MIN_DIMENSION_PRESETS, loadSettings, saveSettings
- No hardcoded MIN_DIMENSION constants remain in imgTags.ts or content.ts
- Content script reads minDimension from SCAN_PAGE message payload
- Popup state includes settings field, loads on mount, passes to scan and download
- GIF filtering applied when skipGifs=true
- CDN rewrite conditional on cdnUpscalingEnabled
- All tests pass (npm test)
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-settings-view-image-quality-control-minimum-resolution-filtering/999.2-01-SUMMARY.md`
</output>
